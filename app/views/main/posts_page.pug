extends main_layout

block content
    include navbar
    div.container-fluid(style="margin-top: 70px")
        div.row
            .col-3
                include user_info
            .col-6
                br
                if searched
                    i
                        h4.text-secondary Pesquisa: '#{searched}'
                if typeof(posts) !== "undefined"
                    each p in posts
                        include post
                        hr
            .col-3
